{% block user_block %}
        <li>
            <div class="navbar-login">
                <div class="row">
                    <div class="col-lg-12">
                        <p class="text-left"><strong>{{ app.user.username }}</strong></p>
                        <p class="text-left small">{{ app.user.email }}</p>
                        <p class="text-left">
                            <a href="{{ path('fos_user_profile_edit') }}" class="btn btn-primary btn-block btn-sm">Edit Profile</a>
                        </p>
                    </div>
                </div>
            </div>
        </li>
        <li class="divider"></li>
        <li>
            <div class="navbar-login navbar-login-session">
                <div class="row">
                    <div class="col-lg-12">
                        <p>
                            <a href="{{ path('fos_user_security_logout') }}" class="btn btn-danger btn-block">Logout</a>
                        </p>
                    </div>
                </div>
            </div>
        </li>

{% endblock %}
